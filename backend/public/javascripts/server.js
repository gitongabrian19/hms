


//  create a function to collect details
//  module.export the function

/* 
In the routes create another root

in app.js create like this 

var submit = require('./routes/submitRoute');
in my routes
app.options("/submit", cors(corsOptions));
app.post("/submit",cors(corsOptions), submit);

*/